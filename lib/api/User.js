  /**
   * @param  {string} id - a unique identifier
   * @param  {string} name - username used in login
   */
  class User {
  constructor(id, name) {
    this.id = id;
    this.name = name;
  }
}

export default User;
